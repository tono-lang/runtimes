# runtimes

Tono runtime libraries: hand-written transport per `(protocol, language)`.
Each library is a dependency of the generated SDK, not of the compiler.

- `ts/` `rust/` `go/` `java/` `python/` — one library per language.
